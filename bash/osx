#OS-X specific

#aliases
alias ls='ls -F -G'
alias vim='mvim -v'

#Latest svn commit with viewcv link
alias lastc='echo "http://source.caseshare.com/viewvc?view=revision&root=svn&revision=$(git svn log -1 --oneline | cut -d" " -f1)" | pbcopy'
